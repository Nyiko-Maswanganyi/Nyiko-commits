def remover(ex_list):
  #list starts with 0 index
  position = 3 - 1 
  idx = 0
  size_list = (len(int_list))
  while len_list>0:
    idx = (position+idx)%size_list
    print(int_list.pop(idx))
    size_list -= 1
 
 
